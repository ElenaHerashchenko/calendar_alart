<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Calendar.Views.ForgottPassword">
    <ContentPage.Content>
        <StackLayout x:Name="friendStack">
            <Label Text="Enter name or email" HorizontalOptions="Center" FontSize="Large" TextColor="Black"/>
            <Entry Text="{Binding Name}" FontSize="Medium" />
            <Button Command="{Binding navigationCommand}" HorizontalOptions="CenterAndExpand"  Text="Sign In" />
            <Button Text="Next" Command="{Binding navigateCommand}"  HorizontalOptions="CenterAndExpand" />
         
        </StackLayout>
    </ContentPage.Content>

</ContentPage>